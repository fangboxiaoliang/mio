package controller

import (
	"testing"
)

func TestBuildConfig(t *testing.T) {
	/*build := new(mocks.BuildAggregate)
	appInfo := newSourceConfigController(nil, nil, nil, nil)
	build.On("Create", &v1alpha1.BuildConfig{}, "", "v1").Return(nil, nil)

	app := web.NewTestApp(t, appInfo).
		Run(t)
	log.SetLevel(log.DebugLevel)
	log.Println(io.GetWorkDir())
	t.Run("should pass with jwt token", func(t *testing.T) {
		app.Post("/build").WithJSON(&command.BuildConfig{}).Expect().Status(http.StatusOK)
	})

	t.Run("should pass with jwt token", func(t *testing.T) {
		app.Get("/build/name/a/namespace/s").Expect().Status(http.StatusOK)
	})*/
}
